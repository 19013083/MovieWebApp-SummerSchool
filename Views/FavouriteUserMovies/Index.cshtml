@model IEnumerable<My_movie_manager.Models.MovieManagerModel.FavouriteUserMovie>

@{
    ViewData["Title"] = "Index";
}

<h1>Favourite movie list</h1>

<h4>These are all your favourite movies</h4>

<p>
    <a asp-action="Create">Add new movie</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                Movie Id
            </th>

            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.MovieId)
            </td>
            <td>
                <a href="/movie/detail/@item.MovieId">View movie details</a> |
                <a asp-action="Edit" asp-route-id="@item.Id">Change</a> |
                <a asp-action="Details" asp-route-id="@item.Id">View entry details</a> |
                <a style="color:red" asp-action="Delete" asp-route-id="@item.Id">Remvoe</a>
            </td>
        </tr>
}
    </tbody>
</table>
